# AA-26 corrected measurement and dependency freeze

Base: d4bd3ff9fa74472353f6db010cee07346c036be0. This rule set precedes dependency refits and candidate-performance inspection. Astra; CPU only. No RunPod or paid/GPU fallback, model inference, prompts, persona scoring, or viewer work. Historical artifacts remain unchanged; new versions live here.

## Broad target version 2

Start from the 79 administered-source constructs documented in AA-13. Resolve signs by the prior AA-1 exact-item-set official-key mapping; its recorded Extraversion exception uses the official 19-item IPIP100extra key (q_55 omitted). Apply 7−x to every negative item. A respondent scale is an observed mean of keyed raw 1–6 responses and is missing unless at least two items are observed. Remove current bridge41 predictor items, all official Big Five predictor items, and all 13 HiFWB outcome items from each target **before freezing any target dimensions or selecting any candidates**. Exclude dimensions with fewer than two remaining items or <2% two-item respondent coverage. Do not impute target labels. Scores are explicitly disjoint-item source-construct subsets, not claimed to have the full original scales' reliability. Record every removed item and dimension, respondent/observed-cell coverage, fingerprints, and source lineage. No factor fitting or named latent factors.

Broad evaluation uses respondents with >=5 observed target dimensions. All baseline/candidate comparisons use this same cohort and observed-target mask. Exact duplicate target vectors/item sets are audited; primary performance is macro-average held-out R² across fixed dimensions, with per-dimension errors/correlations and within-respondent profile correlation (>=5 observed dimensions) secondary. Inverse duplicates, if found, collapse by lexicographic source ID before fitting. No mean-imputed labels or comparisons restricted by which candidate is observed.

## Dependency repair

Prospectively resolve flagged proxy eligibility to AA-16's explicit rule: >=2 observed items for every multi-item proxy; a genuinely single-item bridge proxy retains its documented minimum1. Preserve frozen 41-proxy identities and orientation; no AA-16–19 refitting. Compare old n>=1 proxy and corrected scores on identical source rows, including eligibility sets. Verification requires identical eligible sets and maximum absolute score difference <=1e-10 on jointly observed scores, with <=1e-8 metric tolerance if numerically reproduced. Any changed mask is material; do not hide dropped scores by checking only common finite values.

Preserve each affected human analysis's original split and model recipe, changing proxy masking only. AA-20 uses its original raw-row seed20260915 split and training/refit transformations; AA-22/23/24 preserve their historical whole-cohort item standardization for repair attribution, with its limitation explicit. AA-21 common sensitivity preserves its eligible-row split; do not refit the primary fit. Reconstruct old masks/inputs to verify provenance, but do not rerun unaffected experiments. Rerun affected human predictions/associations only when their original sample-size requirements pass; if a corrected gate fails, report failure, never relax it after results. Transport outputs dependent on changed human weights are stale and remain withdrawn; persona scoring is outside scope. Other superseded outputs not regenerated are explicitly identified rather than silently revalidated.

## Candidate and evaluation freeze (before performance inspection)

Use unused items from documented administered-source construct families and their official signs, excluding the fixed target union, current bridge items, all Big Five items, and HiFWB items. Require >=2 keyed items and >=2 observed respondent items, >=2% coverage. Single-item or ambiguous proxies are not candidate families. Publish exact original wording; do not invent questions. Exact duplicate wording/item-set aliases collapse deterministically; audit near-duplicate wording and shared items. Families with the same documented construct (QB6 emotional stability/EPQr negative emotionality) share a semantic group. Other source facets remain distinct unless absolute residual score correlation >=.80 after removing baseline effects on training rows with >=100 jointly observed scores; sensitivity thresholds .70/.90. Prefer at most one member per group. A redundant-family addition may only be labeled an independent-information sensitivity if inner held-out macro-R² gain exceeds .002; the primary top-five/minimal set remains group-distinct. Shared items across retained source subsets are measured once and explicitly reported; scale labels do not count them as new questions twice.

Use regularized multi-output prediction with a separate ridge coefficient vector and observed-label loss for each target, sharing alpha selected by macro validation R². All predictor item moments, feature means/scales, mean imputation, and missingness indicators are training-only. A ridge alpha grid {1,10,100,1000} is selected inside training data. Nested 5-fold outer CV, 3-fold inner CV, seed20260919; no outer target scores select features. Greedy forward selection evaluates k=1 through 8. Compare bridge41 only, Big Five only, their union, and union+candidate path. If fewer than 8 distinct groups survive a fold, report unavailable distinct k and separately evaluate the unrestricted k=1–8 diagnostic; do not call a redundant set distinct.

The smallest useful set is the smallest k whose nested held-out macro-R² is within .01 absolute R² of the best admissible k=1–8; report .005 and .02 tolerance sensitivity and the relative fraction of best observed gain captured. Do not equate the tolerance definition with a guaranteed 95% fraction. Nonpositive total gain yields k=0, no elicitation recommendation. Report paired respondent bootstrap uncertainty (1000 draws) conditional on OOF fits, outer-fold selection stability and 20 additional training/validation split selection runs; distinguish uncertainty of the selection procedure from post hoc consensus labels. Final published ordering uses full-cohort internal CV, with per-label outer test additions only in folds where selected; fixed-set performance is not claimed from adaptively varying paths.

Big Five secondary reconstruction removes all Big Five source items from bridge proxies (requires >=2 remaining items); no Big Five scores serve as their own predictors. HiFWB uses its frozen signed 13-item >=2 rule and bridge+Big Five controls. Candidate selection uses broad-target loss only. Evaluate secondary outcomes on outcome-eligible rows of the same outer test splits, retaining the broad-selected path. No candidate is called a model trait or already represented in models. Broad profile, Big Five, HiFWB, and later-elicitation readiness are separate conclusions.

## CPU/RunPod decision gate

Read-only source reconstruction and small ridge systems run on the existing local scientific Python environment, with BLAS threads limited to one. No tensor loading and no raw respondent export. If resources or validity fail, stop or report the blocked portion; RunPod/GPU/paid compute and model inference remain prohibited, regardless of statistical result.
