# AA-27 analysis freeze — before human facet distributions or persona values

Agent: GPT-6 Astra. Thinking: High. Base91ad751; policy6d564c3. Human file and official documentation acquired from Johnson OSF; no response values or persona score values inspected to select crosswalks. Source documentation/density auditing is permitted before comparison. Existing AA21/25 semantic mapping is a candidate aid, not evidence of facet validity. The preliminary workbook inspection exposed published UK/Ireland example norms in its first few rows; those norms are not the acquired response distribution and are not used for mappings, gating, scoring, calibration or interpretation. No human correlations, HiFWB, model coordinates or desired classifications enter the crosswalk.

## Human scoring and density gate

Use the 307,313-case IPIP300.dat version2 SHA1bbb7189f5f0f2883bb4493f956cf639178c5c8ee95319fd3881f6d9d2f189a3. DAT300.doc version3 specifies fixed-width columns34–333 as items1–300, 1–5 response scale, 0 missing. The responses are **already reverse-scored**. Preserve those keyed values; reconstruct original-category values as 6−stored only when independently checking key logic, then reapply the same reversal once to recover stored scores. Never reverse the downloaded data a second time.

Primary: all ten valid item responses for each of all30 facets, age18–80 inclusive. Facet score is its ten-item arithmetic mean. Density reporting additionally counts complete profiles at all ages and near-complete profiles with >=9/10 valid responses in every facet. The focused completeness sensitivity allows9/10 per facet and averages observed items; no across-facet imputation. No data-driven exclusions based on profile unusualness. Report missingness, demographics, and counts in aggregate only. Complete adult N must be >=5,000, providing at least1,000 calibration and1,000 held-out humans. The source is a self-selected internet reference, not population norms. No unreported quality filter; possible careless responding remains a limitation.

## Semantic construction and fixed coverage gate

Review all240 canonical definitions against all30 official item families. The full Cartesian table records direct, close, broad or none, polarity, rationale, evidence, confidence, alias group and strict/expanded inclusion. Direct requires explicit correspondence to item content, not merely a broad Big Five relation or an evocative label. Close permits adjacent content with a stated inferential step. Broad/none are excluded from both constructions. The prior AA25 direct labels can be downgraded when they refer only to broad-domain resemblance; no inherited label is binding.

Collapse same-pole near-alias definitions within each facet to one representative, prefer direct over close then canonical lexical order. Opposite-pole descriptors are not automatically collapsed; flag facets with only one represented pole. No numeric correlations choose representatives. A retained facet needs **at least two nonduplicate model traits**. The direct-only primary must retain **>=18/30 facets and >=3 facets in EACH of N,E,O,A,C**. Direct+close uses the same per-facet minimum and is sensitivity only; it cannot rescue a failed primary gate. Freeze the complete crosswalk and coverage result before profile comparisons. If primary coverage fails, stop with “untestable with the present bridge”; do not run a reduced-domain replacement result or relaxed-gate sensitivity.

If primary gate passes, use signed unit means of within-model trait z scores over the frozen representatives; retain the same strict facet space for strict/expanded comparisons. No model values enter this selection. Model labels/persona IDs and 240 trait headers must align exactly across the saved Qwen/Llama/Gemma matrices; no positional join without IDs. Consensus averages direction-aligned model facet z scores, then undergoes the same marginal transform. Five-domain diagnostics average retained facets equally and compare with AA21/25 existing Big Five scores, reversing emotional stability to Neuroticism. No HiFWB columns or fits. Diagnostic gate: every domain in every model must have positive Spearman agreement >=.30 with the saved human-anchored Big Five scores. No refitting crosswalk to improve agreement. Failure stops plausibility interpretation.

## Frozen conditional analysis (execute only if gates pass)

Seed20260920. Deduplicate exact300-item response patterns before split if necessary, retaining the lowest source row internally; export only duplicate counts/hashes. Reproducibly shuffle eligible unique respondents into60% reference /20% calibration /20% held-out. Use up to30,000 reference and10,000 calibration and10,000 held-out selected by the frozen shuffle for bounded CPU nearest-neighbor work; report full eligible and used counts. Human marginal transform: reference empirical midrank CDF, clipped to[.5/(n+1),1−.5/(n+1)] then inverse normal; apply reference transform unchanged to calibration/held-out. Model marginals: within-model275-persona midrank probabilities (rank−.5)/275 then inverse normal, separately for each facet/construction; consensus likewise. This discards marginal prevalence and tests the copula-like configuration of relative standings, not absolute equivalence.

Fit Ledoit–Wolf covariance and mean on human reference only. Global distance is squared regularized Mahalanobis. Whiten by its eigensystem and use Euclidean nearest-reference distance (one nearest neighbor) as complementary local measure; no dimension reduction primary. Report nearest-reference cosine similarity in the standardized facet space as an aggregate per-persona number, never any human ID or vector. Calibrate percentiles against independent human calibration distances using (1+#calibration<=query)/(ncal+1); report held-out-human percentiles and category rates as calibration validation. Human-supported requires both distances <=95th calibration percentile; strongly outlying requires both >99th; all remaining cases borderline. These sample-conditional labels do not imply behavior simulation or human equivalence.

Attribution uses signed contributions z_j*(precision*z)_j, normalized to total global distance; negative contributions possible. Cross-model agreement: same named persona, rank correlations and class concordance, never population-frequency comparisons. Families use already documented persona family/category metadata only; otherwise report family analysis unavailable rather than invent outcome-dependent groups. Illustrations: five lowest and five highest consensus maximum(metric percentile) personas, deterministic persona-name tie break; retain “outlying” wording only if thresholds are met.

Focused sensitivities: direct+close; five-domain instead of retained facets; each model and consensus; 9/10 human completeness;200 calibration-resample bootstrap percentile intervals conditional on fixed reference fit; leave-one-mapped-trait-out for facets with2–3 representatives, evaluated without reselecting mappings, reporting score/rank/class changes. No human-reference refit bootstrap claim. Frozen-seed rerun and independent distance/percentile calculations must agree within1e−10 (1e−8 for floating eigensystems).

## Scope and stop rule

CPU only. No target-model inference, RunPod, paid API/compute, interventions, HiFWB scoring, new model-trait extraction, or viewer changes. Raw respondent data stay gitignored; never save human score rows, identifiers or neighbor records. Preserve AA16–26 artifacts byte-for-byte. If a gate fails, publish feasibility/coverage evidence with downstream outputs explicitly marked not run; do not fabricate calibration/profile figures.
