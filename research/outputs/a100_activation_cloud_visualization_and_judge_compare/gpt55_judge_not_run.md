# GPT-5.5 Judge Not Run

`gpt-5.5` was available in the model list, but the required judge configuration could not run.

Reason: API rejected temperature 0 for gpt-5.5; only default temperature is supported.

The card required temperature 0 and no silent substitution. No alternate model or default-temperature GPT-5.5 run was used.
